#include "main.h"
#include <stdio.h>
/**
 * add -  adds two integers and returns the result
 * @a:parameter to be added
 * @b:parameter to be added
 * Return: 0
 */
int add(int a, int b)
{
	return (a + b);
}
