#include "main.h"
#include <stdio.h>
/**
 * mul - that multiplies two integers
 * @a: parameter to be multiplied
 * @b: parameter to be multiplied
 * Return: product of the two integers multiplied
 */
int mul(int a, int b)
{
	return (a * b);
}
