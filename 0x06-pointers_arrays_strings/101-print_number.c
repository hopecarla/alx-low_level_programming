#include "main.h"
/**
 * print_number - that prints an integer
 * @n: number to be checked
 * Return: 0
 */
void print_number(int n)
{
	if (n < 0)
	{
		_putchar('-');
	n = -n;
	}

	if (n - n  == 0)
	{
		_putchar('0');
	}

}
